# enum class Bitmask

1. docker build -t clang-p2996-bazel -f docker/Dockerfile docker
1. docker run -ti --rm -v (pwd):/work -w /work clang-p2996-bazel
1. bazel test //src/...
1. bazel run //src:bitmask_operators_example


